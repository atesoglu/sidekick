# widgets

Dashboard-specific widgets (charts, metric tiles, activity feeds, etc.) live
here once the developer productivity dashboard work starts. Each widget
should be self-contained (its own data fetching via `src/services`) and
composed into pages from `src/pages`. Nothing lives here yet.
