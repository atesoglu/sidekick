# components

Reusable, presentational UI building blocks shared across pages/widgets
(e.g. cards, status badges). Prefer composing Carbon components here over
introducing a second component library. Nothing lives here yet.
