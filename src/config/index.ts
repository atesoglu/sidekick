/** Static, non-secret application configuration. */
export const APP_NAME = "Sidekick";

export const NAV_ITEMS = [
  { label: "Text Editor", path: "/" },
  { label: "Home", path: "/home" },
] as const;
